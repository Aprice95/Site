---
import ProductLayout from '../../layouts/ProductLayout.astro';
import { products } from '../../data/products';

const product = products.find((entry) => entry.slug === 'marching-tycoon');
if (!product) {
  throw new Error('Marching Tycoon product data missing.');
}
---

<ProductLayout product={product} />
